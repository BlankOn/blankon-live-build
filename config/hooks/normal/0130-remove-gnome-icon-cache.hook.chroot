#!/bin/sh

set -e

# Rebuild icon cache to ensure icons load properly.

for dir in /usr/share/icons/*/; do
	if [ -f "${dir}index.theme" ]; then
		gtk-update-icon-cache -f -q "${dir}" 2>/dev/null || true
	fi
done
