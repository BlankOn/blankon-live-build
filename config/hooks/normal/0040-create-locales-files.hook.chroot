#!/bin/sh
set -e

# Generate only essential locales (fewer = faster ldconfig on live boot)
echo "id_ID.UTF-8 UTF-8" >> /etc/locale.gen
echo "en_US.UTF-8 UTF-8" >> /etc/locale.gen
locale-gen

# Keep LANG out of /etc/environment; the locale belongs in
# /etc/default/locale alone, so a later desktop install can override it
# per-user. Only PATH and other non-locale vars belong here.
cat > /etc/environment << 'EOF'
PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
EOF

# Set default locale only via /etc/default/locale
cat > /etc/default/locale << 'EOF'
LANG=id_ID.UTF-8
LANGUAGE=id_ID:id
EOF
